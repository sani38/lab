#include <iostream>
#include <math.h>
using namespace std;

int main() {
    int a, b;
    cout;
    cin >> a;
    cout;
    cin >> b;
    double c = sqrt(a * a + b * b);
    cout << c << endl;
    return 0;
}
